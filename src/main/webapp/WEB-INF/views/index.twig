<html>
  <head>
    <title>index</title>
  </head>
  <body>

  {% macro input(name, value, type, size) %}
      <input type="{{ type|default('text') }}" name="{{ name }}" value="{{ value|e }}" size="{{ size|default(20) }}" />
  {% endmacro %}

  {% import _self as forms %}
  <p>{{ forms.input('username', "", "number", "20") }}</p>

    {% if name %}
    <h1>Hello {{ name }}!</h1>
    {% else %}
    <h1>index edited edited!</h1>
    {% endif %}
  </body>
</html>